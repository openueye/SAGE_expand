"""Checkpoint identities accepted by SAGE."""

CHECKPOINT_VERSION = "sage-checkpoint-v1"
SOURCE_CHECKPOINT_VERSIONS = {CHECKPOINT_VERSION}
APPEARANCE_REFINEMENT_CHECKPOINT_VERSION = (
    "sage-refinement-checkpoint-v1"
)
